<!DOCTYPE html>
<html lang="zh-HK">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= factorName %>分析 - 場次 <%= raceNo %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        .score-badge { font-weight: bold; font-size: 1.1em; }
        .detail-row { font-size: 0.9em; color: #555; }
        .config-panel { background: #f8f9fa; padding: 15px; border-radius: 8px; margin-bottom: 20px; border: 1px solid #dee2e6; }
    </style>
</head>
<body class="bg-light">
    <div class="container py-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <a href="/analysis/race/<%= raceId %>?<%= queryString %>" class="btn btn-outline-secondary btn-sm mb-2">
                    <i class="bi bi-arrow-left"></i> 返回總表
                </a>
                <h2><%= factorName %>分析 (場次 <%= raceNo %>)</h2>
                <span class="badge bg-info text-dark"><%= venue %></span>
            </div>
            <button class="btn btn-primary" type="button" data-bs-toggle="collapse" data-bs-target="#configCollapse">
                <i class="bi bi-sliders"></i> 設定<%= factorName %>權重
            </button>
        </div>

        <!-- Configuration Panel -->
        <div class="collapse show" id="configCollapse">
            <div class="config-panel">
                <form id="weightForm" action="/analysis/race/<%= raceId %>/factor/<%= factor %>" method="GET">
                    <!-- Preserve main weights -->
                    <input type="hidden" name="w_form" value="<%= weights.form %>">
                    <input type="hidden" name="w_jockey" value="<%= weights.jockey %>">
                    <input type="hidden" name="w_cd" value="<%= weights.courseDistance %>">
                    <input type="hidden" name="w_track" value="<%= weights.trackwork %>">
                    <input type="hidden" name="w_draw" value="<%= weights.draw %>">
                    <input type="hidden" name="w_class" value="<%= weights.class %>">
                    <input type="hidden" name="w_rating" value="<%= weights.rating %>">
                    <input type="hidden" name="w_partner" value="<%= weights.partnership %>">
                    <input type="hidden" name="w_trial" value="<%= weights.barrierTrial %>">
                    
                    <div class="row g-3 align-items-end">
                        <% if (factor === 'form') { %>
                            <div class="col-md-2">
                                <label class="form-label">頭馬分數 (Win)</label>
                                <input type="number" class="form-control" name="sw_form_win" value="<%= subWeights.form.win %>">
                            </div>
                            <div class="col-md-2">
                                <label class="form-label">第二名 (2nd)</label>
                                <input type="number" class="form-control" name="sw_form_p2" value="<%= subWeights.form.place2 %>">
                            </div>
                            <div class="col-md-2">
                                <label class="form-label">第三名 (3rd)</label>
                                <input type="number" class="form-control" name="sw_form_p3" value="<%= subWeights.form.place3 %>">
                            </div>
                            <div class="col-md-2">
                                <label class="form-label">第四名 (4th)</label>
                                <input type="number" class="form-control" name="sw_form_p4" value="<%= subWeights.form.place4 %>">
                            </div>
                            <div class="col-md-2">
                                <label class="form-label">第五/六名</label>
                                <input type="number" class="form-control" name="sw_form_p56" value="<%= subWeights.form.place5_6 %>">
                            </div>
                        <% } else if (factor === 'jockey') { %>
                            <div class="col-md-4">
                                <label class="form-label">近績權重 (0.0 - 1.0)</label>
                                <input type="number" step="0.1" max="1" min="0" class="form-control" name="sw_jockey_recent" value="<%= subWeights.jockey.recentFormWeight %>">
                                <div class="form-text">近期手風佔比</div>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">人馬合作權重 (0.0 - 1.0)</label>
                                <input type="number" step="0.1" max="1" min="0" class="form-control" name="sw_jockey_hist" value="<%= subWeights.jockey.historyWeight %>">
                                <div class="form-text">過往合作成績佔比</div>
                            </div>
                        <% } else if (factor === 'trackwork') { %>
                            <div class="col-md-3">
                                <label class="form-label">快操/試閘分數</label>
                                <input type="number" class="form-control" name="sw_track_fast" value="<%= subWeights.trackwork.fastWork %>">
                            </div>
                            <div class="col-md-3">
                                <label class="form-label">快踱分數</label>
                                <input type="number" class="form-control" name="sw_track_trot" value="<%= subWeights.trackwork.trotWork %>">
                            </div>
                            <div class="col-md-3">
                                <label class="form-label">踱步/慢操分數</label>
                                <input type="number" class="form-control" name="sw_track_slow" value="<%= subWeights.trackwork.slowWork %>">
                            </div>
                        <% } else if (factor === 'class') { %>
                            <div class="col-md-3">
                                <label class="form-label">降班加分 (Dropper)</label>
                                <input type="number" class="form-control" name="sw_class_drop" value="<%= subWeights.class.dropper %>">
                            </div>
                            <div class="col-md-3">
                                <label class="form-label">升班分數 (Riser)</label>
                                <input type="number" class="form-control" name="sw_class_rise" value="<%= subWeights.class.riser %>">
                            </div>
                            <div class="col-md-3">
                                <label class="form-label">同班分數 (Same)</label>
                                <input type="number" class="form-control" name="sw_class_same" value="<%= subWeights.class.same %>">
                            </div>
                        <% } else if (factor === 'rating') { %>
                            <div class="col-md-3">
                                <label class="form-label">低於贏馬評分</label>
                                <input type="number" class="form-control" name="sw_rating_below" value="<%= subWeights.rating.belowWin %>">
                            </div>
                            <div class="col-md-3">
                                <label class="form-label">接近贏馬評分 (+5)</label>
                                <input type="number" class="form-control" name="sw_rating_near" value="<%= subWeights.rating.nearWin %>">
                            </div>
                            <div class="col-md-3">
                                <label class="form-label">高於贏馬評分</label>
                                <input type="number" class="form-control" name="sw_rating_above" value="<%= subWeights.rating.aboveWin %>">
                            </div>
                        <% } else { %>
                            <div class="alert alert-info">此項目暫無細項權重設定。</div>
                        <% } %>

                        <div class="col-md-2">
                            <button type="submit" class="btn btn-success w-100">更新計算</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>

        <!-- Data Table -->
        <div class="card shadow-sm">
            <div class="card-body p-0">
                <table class="table table-hover table-striped mb-0 align-middle">
                    <thead class="table-dark">
                        <tr>
                            <th width="5%">#</th>
                            <th width="10%">馬號/馬名</th>
                            <th width="10%" class="text-center"><%= factorName %>分數</th>
                            <th width="10%" class="text-center">總分</th>
                            <th>詳細數據</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% data.forEach((item, index) => { %>
                        <tr>
                            <td><%= index + 1 %></td>
                            <td>
                                <div class="fw-bold"><%= item.horseNo || '-' %></div>
                                <a href="/horse/<%= item.horseName %>" class="text-decoration-none"><%= item.horseName %></a>
                            </td>
                            <td class="text-center">
                                <span class="badge bg-primary rounded-pill fs-6"><%= item.factorScore %></span>
                            </td>
                            <td class="text-center text-muted">
                                <%= item.totalScore %>
                            </td>
                            <td>
                                <% if (factor === 'form') { %>
                                    <% if (item.details.history && item.details.history.length > 0) { %>
                                        <div class="d-flex flex-wrap gap-2">
                                            <% item.details.history.forEach(run => { %>
                                                <span class="badge <%= run.rank == 1 ? 'bg-warning text-dark' : (run.rank <= 3 ? 'bg-success' : 'bg-secondary') %>" 
                                                      title="<%= run.summary %>">
                                                    <%= run.date %>: <%= run.rank %> (<%= run.points %>分)
                                                </span>
                                            <% }); %>
                                        </div>
                                    <% } else { %>
                                        <span class="text-muted">無近績</span>
                                    <% } %>

                                <% } else if (factor === 'jockey') { %>
                                    <div><strong><%= item.details.jockey %></strong></div>
                                    <div class="small mt-1">
                                        <span class="text-primary">近績 (<%= item.details.recentRides %>場): <%= item.details.recentScore %>分</span> | 
                                        <span class="text-success">合作 (<%= item.details.historyRides %>場): <%= item.details.historyScore %>分</span>
                                    </div>
                                    <% if (item.details.recentRidesList && item.details.recentRidesList.length > 0) { %>
                                        <div class="mt-1 text-muted" style="font-size: 0.8em;">
                                            近期: 
                                            <% item.details.recentRidesList.slice(0, 5).forEach(r => { %>
                                                <%= r.place %> 
                                            <% }); %>
                                            ...
                                        </div>
                                    <% } %>

                                <% } else if (factor === 'trackwork') { %>
                                    <div>
                                        總操練: <%= item.details.totalWorkouts %> | 快操: <%= item.details.fastWorkouts %>
                                    </div>
                                    <% if (item.details.recentFastWork && item.details.recentFastWork.length > 0) { %>
                                        <div class="mt-1 small text-secondary">
                                            <% item.details.recentFastWork.forEach(log => { %>
                                                <div><%= log %></div>
                                            <% }); %>
                                        </div>
                                    <% } %>

                                <% } else if (factor === 'class') { %>
                                    <div>
                                        <%= item.details.comment %> 
                                        (現: <%= item.details.currentClass %>, 前: <%= item.details.lastClass %>)
                                    </div>

                                <% } else if (factor === 'rating') { %>
                                    <div>
                                        <%= item.details.comment %>
                                        (現: <%= item.details.currentRating %>, 贏: <%= item.details.lastWinRating || 'NA' %>)
                                    </div>

                                <% } else if (factor === 'draw') { %>
                                    <div>
                                        <strong>檔位: <%= item.details.draw %></strong>
                                        <div class="text-muted small"><%= item.details.comment %></div>
                                    </div>

                                <% } else if (factor === 'courseDistance') { %>
                                    <div>
                                        <% if (typeof item.details === 'string') { %>
                                            <span class="text-muted"><%= item.details %></span>
                                        <% } else { %>
                                            <strong>同程往績: <%= item.details.matchingRuns %> 場</strong>
                                            <div class="small text-success">
                                                平均分: <%= Math.round(item.details.avgPoints) %>
                                            </div>
                                        <% } %>
                                    </div>

                                <% } else if (factor === 'partnership') { %>
                                    <div>
                                        <% if (typeof item.details === 'string') { %>
                                            <span class="text-muted"><%= item.details %></span>
                                        <% } else { %>
                                            <strong><%= item.details.jockey %> + <%= item.details.trainer %></strong>
                                            <div class="small">
                                                合作 <%= item.details.collaborations %> 場, 平均分: <%= item.details.avgScore %>
                                            </div>
                                        <% } %>
                                    </div>

                                <% } else if (factor === 'barrierTrial') { %>
                                    <div>
                                        <% if (typeof item.details === 'string') { %>
                                            <span class="text-muted"><%= item.details %></span>
                                        <% } else { %>
                                            <strong>最近試閘: <%= item.details.rank %> / <%= item.details.totalHorses %></strong>
                                            <div class="small text-muted">
                                                日期: <%= item.details.lastTrialDate %>
                                            </div>
                                        <% } %>
                                    </div>

                                <% } else { %>
                                    <pre class="m-0 small"><%= JSON.stringify(item.details, null, 2) %></pre>
                                <% } %>
                            </td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
