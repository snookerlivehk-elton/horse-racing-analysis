<!DOCTYPE html>
<html lang="zh-HK">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HKJC 排位資料更新 (Data Update)</title>
    <style>
        :root {
            --bg-dark: #1e1e1e;
            --bg-panel: #2d2d2d;
            --text-main: #e0e0e0;
            --text-muted: #a0a0a0;
            --accent-gold: #fbbf24;
            --accent-green: #10b981;
            --accent-blue: #3b82f6;
            --border-color: #404040;
        }

        body { 
            font-family: 'Roboto', 'Segoe UI', Tahoma, sans-serif; 
            padding: 0; 
            background-color: var(--bg-dark); 
            color: var(--text-main);
            margin: 0; 
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 { 
            color: var(--accent-gold); 
            text-align: center; 
            margin: 40px 0 20px; 
            font-size: 1.8em;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .container {
            width: 90%;
            max-width: 600px;
            background: var(--bg-panel);
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.5);
            text-align: center;
            border: 1px solid var(--border-color);
        }

        .meta-info { 
            margin-bottom: 30px; 
            color: var(--text-muted); 
            font-size: 0.9em; 
            line-height: 1.6;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 20px;
        }

        .btn-bar { 
            display: flex; 
            flex-direction: column; 
            gap: 15px; 
            width: 100%;
        }

        .btn { 
            padding: 15px; 
            border-radius: 8px; 
            border: none; 
            cursor: pointer; 
            font-size: 1em; 
            font-weight: 600; 
            transition: all 0.2s; 
            text-transform: uppercase;
            text-decoration: none;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .btn-primary { 
            background-color: var(--accent-blue); 
            color: white; 
            box-shadow: 0 4px 6px rgba(59, 130, 246, 0.3);
        }
        .btn-primary:hover { 
            background-color: #2563eb; 
            transform: translateY(-2px);
        }
        .btn-primary:active { transform: translateY(0); }

        .btn-warning {
            background-color: var(--accent-gold);
            color: black;
        }
        .btn-warning:hover { background-color: #d97706; }

        .btn-link { 
            background: transparent; 
            color: var(--text-muted); 
            margin-top: 10px;
            font-size: 0.9em;
        }
        .btn-link:hover { color: white; text-decoration: underline; }

        .error { 
            color: #f87171; 
            background: rgba(220, 38, 38, 0.1); 
            padding: 15px; 
            border: 1px solid #dc2626; 
            border-radius: 8px; 
            text-align: left; 
            margin-bottom: 20px; 
            font-size: 0.9em;
        }
        
        .loading {
            margin-top: 20px;
            padding: 20px;
            background: rgba(0,0,0,0.2);
            border-radius: 8px;
        }
        .spinner {
            border: 4px solid rgba(255, 255, 255, 0.1);
            border-left: 4px solid var(--accent-blue);
            border-radius: 50%;
            width: 30px;
            height: 30px;
            animation: spin 1s linear infinite;
            margin: 0 auto 10px;
        }
        @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }

        .status-card {
            background: rgba(16, 185, 129, 0.1);
            border: 1px solid var(--accent-green);
            padding: 15px;
            border-radius: 8px;
            margin-top: 20px;
            color: var(--accent-green);
            display: none;
        }
    </style>
</head>
<body>

    <h1>🐎 資料更新中心 (Data Center)</h1>

    <div class="container">
        <div class="meta-info">
            <div><strong>Server Version:</strong> <%= serverVersion %></div>
            <div><strong>最後更新 (Last Updated):</strong> <%= lastUpdated ? new Date(lastUpdated).toLocaleString() : '尚未爬取 (No Data)' %></div>
            <% if (scrapeResult && scrapeResult.raceDate) { %>
                <div style="margin-top: 10px; font-size: 1.1em; color: var(--accent-gold);">
                    📅 當前賽事: <%= scrapeResult.raceDate %>
                </div>
            <% } %>
        </div>

        <% if (scrapeError) { %>
            <div class="error">
                <strong>⚠️ 上次更新錯誤:</strong><br>
                <%= scrapeError %>
            </div>
        <% } %>

        <div class="btn-bar">
            <button class="btn btn-primary" onclick="triggerScrape()">
                <span>🔄</span> 立即更新資料 (Update Data)
            </button>
            
            <a href="/speedpro" class="btn btn-warning">
                <span>📊</span> 查看速勢分析 (SpeedPro)
            </a>
            
            <a href="/" class="btn-link">🏠 返回首頁 (Home)</a>
        </div>

        <div id="loading" class="loading" style="display: none;">
            <div class="spinner"></div>
            <p>正在從 HKJC 獲取最新資料，請稍候...<br><span style="font-size:0.8em; color: #aaa;">(Fetching data from HKJC...)</span></p>
        </div>

        <div id="status-card" class="status-card">
            ✅ 更新成功！頁面即將重新整理...
        </div>
    </div>

    <script>
        function triggerScrape() {
            // if (!confirm('確定要更新排位表數據嗎？ (Update Racecard Data?)')) return;
            
            const loading = document.getElementById('loading');
            const btnBar = document.querySelector('.btn-bar');
            const statusCard = document.getElementById('status-card');
            
            loading.style.display = 'block';
            btnBar.style.opacity = '0.5';
            btnBar.style.pointerEvents = 'none'; // Disable buttons
            
            fetch('/api/scrape-race-data')
                .then(res => res.json())
                .then(data => {
                    loading.style.display = 'none';
                    btnBar.style.opacity = '1';
                    btnBar.style.pointerEvents = 'auto';

                    if (data.error) {
                        alert('❌ 更新失敗 (Update Failed): ' + data.error);
                    } else {
                        statusCard.style.display = 'block';
                        statusCard.innerHTML = `
                            <strong>✅ 更新成功 (Success)!</strong><br>
                            📅 賽事日期: ${data.raceDate || 'Unknown'}<br>
                            🐴 已儲存馬匹: ${data.dbStatus?.saved || 0}
                        `;
                        // Reload after a short delay to show the success message
                        setTimeout(() => {
                            location.reload();
                        }, 2000);
                    }
                })
                .catch(err => {
                    loading.style.display = 'none';
                    btnBar.style.opacity = '1';
                    btnBar.style.pointerEvents = 'auto';
                    alert('❌ 請求失敗 (Request Failed): ' + err.message);
                });
        }
    </script>
</body>
</html>
