<!DOCTYPE html>
<html lang="zh-HK">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>賽事分析 - 第 <%= raceNo %> 場 (<%= raceId %>)</title>
    <style>
        :root {
            --bg-dark: #1e1e1e;
            --bg-panel: #2d2d2d;
            --text-main: #e0e0e0;
            --text-muted: #a0a0a0;
            --accent-gold: #fbbf24;
            --accent-green: #10b981;
            --accent-blue: #3b82f6;
            --accent-red: #ef4444;
            --border-color: #404040;
            --table-header-bg: #333333;
            --table-row-even: #252525;
            --table-row-odd: #2a2a2a;
        }

        body { 
            font-family: 'Roboto', 'Segoe UI', Tahoma, sans-serif; 
            padding: 20px; 
            background-color: var(--bg-dark); 
            color: var(--text-main);
            margin: 0; 
            min-height: 100vh;
        }

        .container {
            max-width: 1400px; /* Wider for analysis table */
            margin: 0 auto;
        }

        .header {
            margin-bottom: 30px;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        h1 { 
            color: var(--accent-gold); 
            margin: 0;
            font-size: 2em;
        }

        .meta-info {
            color: var(--text-muted);
            font-size: 1.1em;
            margin-top: 5px;
        }

        .table-container {
            background: var(--bg-panel);
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0,0,0,0.3);
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 0.9em;
            white-space: nowrap;
        }

        th, td {
            padding: 10px 12px;
            text-align: center;
        }

        th {
            background-color: var(--table-header-bg);
            color: var(--accent-gold);
            font-weight: 600;
            position: sticky;
            top: 0;
            z-index: 10;
        }

        /* Freeze first column (Rank/Horse) */
        th:first-child, td:first-child {
            position: sticky;
            left: 0;
            z-index: 11;
            background-color: inherit; /* Inherit row bg */
        }
        th:nth-child(2), td:nth-child(2) {
            position: sticky;
            left: 40px; /* Approx width of rank */
            z-index: 11;
            background-color: inherit;
        }

        tr:nth-child(even) { background-color: var(--table-row-even); }
        tr:nth-child(odd) { background-color: var(--table-row-odd); }
        tr:hover { background-color: #383838; }

        .score-cell {
            font-weight: bold;
            font-size: 1.1em;
        }

        .score-high { color: var(--accent-green); }
        .score-mid { color: var(--accent-gold); }
        .score-low { color: var(--text-muted); }

        .rank-1 { color: #FFD700; font-weight: bold; }
        .rank-2 { color: #C0C0C0; font-weight: bold; }
        .rank-3 { color: #CD7F32; font-weight: bold; }

        .detail-cell {
            font-size: 0.85em;
            color: var(--text-muted);
            max-width: 150px;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        /* Tooltip for details */
        .tooltip {
            position: relative;
            cursor: help;
            border-bottom: 1px dotted var(--text-muted);
        }
        
        .tooltip:hover::after {
            content: attr(data-tip);
            position: absolute;
            bottom: 100%;
            left: 50%;
            transform: translateX(-50%);
            background: #000;
            color: #fff;
            padding: 5px 10px;
            border-radius: 4px;
            white-space: pre-wrap;
            z-index: 20;
            min-width: 200px;
            text-align: left;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div>
                <h1>賽事分析列表</h1>
                <div class="meta-info">
                    第 <%= raceNo %> 場 | <%= raceId %> | <%= venue %> | <%= distance %>M | <%= course %>
                </div>
            </div>
            <div>
                <a href="/" style="color: var(--accent-blue); text-decoration: none;">返回首頁</a>
            </div>
        </div>

        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th style="width: 40px;">排名</th>
                        <th style="min-width: 120px; text-align: left;">馬名</th>
                        <th style="min-width: 60px;">總分</th>
                        
                        <!-- Factor Columns -->
                        <th class="sortable" data-sort="form">
                            <a href="/analysis/race/<%= raceId %>/factor/form?<%= queryString %>" class="text-white text-decoration-none" title="點擊查看往績詳情及設定">
                                往績 (<%= Math.round(weights.form * 100) %>%) <i class="bi bi-gear-fill" style="font-size: 0.8em; opacity: 0.7;"></i>
                            </a>
                        </th>
                        <th class="sortable" data-sort="jockey">
                            <a href="/analysis/race/<%= raceId %>/factor/jockey?<%= queryString %>" class="text-white text-decoration-none" title="點擊查看騎師詳情及設定">
                                騎師 (<%= Math.round(weights.jockey * 100) %>%) <i class="bi bi-gear-fill" style="font-size: 0.8em; opacity: 0.7;"></i>
                            </a>
                        </th>
                        <th class="sortable" data-sort="cd">
                            <a href="/analysis/race/<%= raceId %>/factor/courseDistance?<%= queryString %>" class="text-white text-decoration-none">
                                路程 (<%= Math.round(weights.courseDistance * 100) %>%)
                            </a>
                        </th>
                        <th class="sortable" data-sort="track">
                            <a href="/analysis/race/<%= raceId %>/factor/trackwork?<%= queryString %>" class="text-white text-decoration-none">
                                晨操 (<%= Math.round(weights.trackwork * 100) %>%)
                            </a>
                        </th>
                        <th class="sortable" data-sort="draw">
                            <a href="/analysis/race/<%= raceId %>/factor/draw?<%= queryString %>" class="text-white text-decoration-none">
                                檔位 (<%= Math.round(weights.draw * 100) %>%)
                            </a>
                        </th>
                        <th class="sortable" data-sort="class">
                            <a href="/analysis/race/<%= raceId %>/factor/class?<%= queryString %>" class="text-white text-decoration-none">
                                班次 (<%= Math.round(weights.class * 100) %>%)
                            </a>
                        </th>
                        <th class="sortable" data-sort="rating">
                            <a href="/analysis/race/<%= raceId %>/factor/rating?<%= queryString %>" class="text-white text-decoration-none">
                                評分 (<%= Math.round(weights.rating * 100) %>%)
                            </a>
                        </th>
                        <th class="sortable" data-sort="partner">
                            <a href="/analysis/race/<%= raceId %>/factor/partnership?<%= queryString %>" class="text-white text-decoration-none">
                                拍檔 (<%= Math.round(weights.partnership * 100) %>%)
                            </a>
                        </th>
                        <th class="sortable" data-sort="barrierTrial">
                            <a href="/analysis/race/<%= raceId %>/factor/barrierTrial" style="color: inherit; text-decoration: none;">
                                試閘 (<%= Math.round(weights.barrierTrial * 100) %>%)
                            </a>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    <% scores.forEach((horse, index) => { %>
                        <tr>
                            <td class="<%= index < 3 ? 'rank-' + (index + 1) : '' %>"><%= index + 1 %></td>
                            <td style="text-align: left; font-weight: bold;">
                                <a href="/horse/<%= horse.horseId %>" style="color: inherit; text-decoration: none; hover: underline;">
                                    <%= horse.horseName %>
                                </a>
                                <div style="font-size: 0.8em; color: var(--text-muted); font-weight: normal;">
                                    <%= horse.jockey %> | <%= horse.trainer %>
                                </div>
                            </td>
                            <td class="score-cell <%= horse.totalScore >= 60 ? 'score-high' : (horse.totalScore >= 40 ? 'score-mid' : 'score-low') %>">
                                <%= horse.totalScore %>
                            </td>

                            <!-- Form -->
                            <td>
                                <span class="tooltip" data-tip="<%= JSON.stringify(horse.breakdown.details.form || {}, null, 2) %>">
                                    <%= horse.breakdown.formScore %>
                                </span>
                            </td>
                            <!-- Jockey -->
                            <td>
                                <span class="tooltip" data-tip="<%= JSON.stringify(horse.breakdown.details.jockey || {}, null, 2) %>">
                                    <%= horse.breakdown.jockeyScore %>
                                </span>
                            </td>
                            <!-- Course/Dist -->
                            <td>
                                <span class="tooltip" data-tip="<%= JSON.stringify(horse.breakdown.details.courseDist || {}, null, 2) %>">
                                    <%= horse.breakdown.courseDistScore %>
                                </span>
                            </td>
                            <!-- Draw -->
                            <td>
                                <span class="tooltip" data-tip="Draw: <%= horse.draw %>&#10;<%= JSON.stringify(horse.breakdown.details.draw || {}, null, 2) %>">
                                    <%= horse.breakdown.drawScore %>
                                </span>
                            </td>
                            <!-- Class -->
                            <td>
                                <span class="tooltip" data-tip="<%= JSON.stringify(horse.breakdown.details.class || {}, null, 2) %>">
                                    <%= horse.breakdown.classScore %>
                                </span>
                            </td>
                            <!-- Rating -->
                            <td>
                                <span class="tooltip" data-tip="<%= JSON.stringify(horse.breakdown.details.rating || {}, null, 2) %>">
                                    <%= horse.breakdown.ratingScore %>
                                </span>
                            </td>
                            <!-- Trackwork -->
                            <td>
                                <span class="tooltip" data-tip="<%= JSON.stringify(horse.breakdown.details.trackwork || {}, null, 2) %>">
                                    <%= horse.breakdown.trackworkScore %>
                                </span>
                            </td>
                            <!-- Partnership -->
                            <td>
                                <span class="tooltip" data-tip="<%= JSON.stringify(horse.breakdown.details.partnership || {}, null, 2) %>">
                                    <%= horse.breakdown.partnershipScore %>
                                </span>
                            </td>
                            <!-- Trial -->
                            <td>
                                <span class="tooltip" data-tip="<%= JSON.stringify(horse.breakdown.details.trial || {}, null, 2) %>">
                                    <%= horse.breakdown.trialScore %>
                                </span>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
        
        <div style="margin-top: 20px; color: var(--text-muted); font-size: 0.9em;">
            * 分數僅供參考，滑鼠懸停於分數上可查看詳細數據。
        </div>
    </div>

    <!-- Settings Modal -->
    <div id="settingsModal" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.7); z-index: 100; justify-content: center; align-items: center;">
        <div style="background: var(--bg-panel); padding: 20px; border-radius: 8px; width: 400px; border: 1px solid var(--border-color);">
            <h2 style="margin-top: 0; color: var(--accent-gold);">設定評分權重</h2>
            <p style="color: var(--text-muted); font-size: 0.9em;">總和必須為 100%</p>
            
            <div id="weightInputs">
                <!-- Inputs will be generated by JS -->
            </div>

            <div style="margin-top: 15px; font-weight: bold; text-align: right;">
                總和: <span id="totalWeight">100</span>%
            </div>

            <div style="margin-top: 20px; display: flex; justify-content: flex-end; gap: 10px;">
                <button onclick="closeSettings()" style="padding: 8px 16px; background: transparent; border: 1px solid var(--text-muted); color: var(--text-main); border-radius: 4px; cursor: pointer;">取消</button>
                <button onclick="applyWeights()" style="padding: 8px 16px; background: var(--accent-green); border: none; color: white; border-radius: 4px; cursor: pointer;">應用</button>
            </div>
        </div>
    </div>

    <script>
        const currentWeights = <%- JSON.stringify(weights) %>;
        
        function openSettings() {
            const container = document.getElementById('weightInputs');
            container.innerHTML = '';
            
            const labels = {
                form: "往績",
                jockey: "騎師",
                courseDistance: "路程",
                draw: "檔位",
                class: "班次",
                rating: "評分",
                trackwork: "晨操",
                partnership: "合作",
                barrierTrial: "試閘"
            };

            for (const [key, val] of Object.entries(currentWeights)) {
                if (key === 'daysLookback') continue; // Skip non-weight config
                
                const percent = Math.round(val * 100);
                const div = document.createElement('div');
                div.style.marginBottom = '10px';
                div.style.display = 'flex';
                div.style.justifyContent = 'space-between';
                div.innerHTML = `
                    <label>${labels[key] || key}</label>
                    <input type="number" id="w_${key}" value="${percent}" min="0" max="100" style="width: 60px; background: #333; color: white; border: 1px solid #555; padding: 4px;" onchange="updateTotal()">
                `;
                container.appendChild(div);
            }
            
            document.getElementById('settingsModal').style.display = 'flex';
            updateTotal();
        }

        function closeSettings() {
            document.getElementById('settingsModal').style.display = 'none';
        }

        function updateTotal() {
            let sum = 0;
            const inputs = document.querySelectorAll('#weightInputs input');
            inputs.forEach(input => sum += parseInt(input.value || 0));
            
            const totalEl = document.getElementById('totalWeight');
            totalEl.textContent = sum;
            totalEl.style.color = sum === 100 ? 'var(--accent-green)' : 'var(--accent-red)';
        }

        function applyWeights() {
            let sum = 0;
            const inputs = document.querySelectorAll('#weightInputs input');
            const params = new URLSearchParams(window.location.search);
            
            inputs.forEach(input => {
                const val = parseInt(input.value || 0);
                sum += val;
                // key is w_form, w_jockey etc.
                // Backend expects 0.20, so we pass raw percent here and backend divides? 
                // Wait, my backend implementation expects values like 0.2. 
                // BUT my parser in server.ts: parseFloat(query.w_form).
                // If I send ?w_form=20, backend gets 20.
                // In parseScoringConfig: form: parseFloat(query.w_form) || 0
                // If I send 20, it becomes 20. 
                // BUT in ScoringEngine, it does: score * this.config.weights.form
                // If weight is 20, score will be HUGE.
                // I need to divide by 100 in the backend parser or send 0.20 from frontend.
                // Let's send 0.20 from frontend.
                
                const key = input.id; // w_form
                // params.set(key, val / 100); // Send 0.2
                // Wait, floating point issues in URL? 
                // Better to send "20" and handle division in backend?
                // Let's check backend parser again.
            });

            if (sum !== 100) {
                alert(`總和必須為 100% (目前: ${sum}%)`);
                return;
            }

            // Let's send 0.2 etc.
             inputs.forEach(input => {
                const val = parseInt(input.value || 0);
                const key = input.id;
                // Special mapping for courseDistance -> w_cd
                let paramKey = key;
                if (key === 'w_courseDistance') paramKey = 'w_cd';
                if (key === 'w_trackwork') paramKey = 'w_track';
                if (key === 'w_partnership') paramKey = 'w_partner';
                if (key === 'w_barrierTrial') paramKey = 'w_trial';
                
                params.set(paramKey, val / 100);
            });

            window.location.search = params.toString();
        }
    </script>
</body>
</html>
